version: '3.9'

services:
  postgres:
    image: postgres:16-alpine
    container_name: homolog_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: homolog
      POSTGRES_PASSWORD: homolog_secret
      POSTGRES_DB: homologacao
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U homolog -d homologacao"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
