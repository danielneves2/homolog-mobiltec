{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npx prisma generate --schema=sistema/backend/prisma/schema.prisma && npm --prefix sistema/frontend install --include=dev && npm run build --prefix sistema/frontend",
  "outputDirectory": "sistema/frontend/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    },
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
